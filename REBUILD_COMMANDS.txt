========================================
  FLIXYGO - REBUILD WITH FIXES
========================================

🔴 FIXES APPLIED:
1. ✅ App force close fixed (error handling added)
2. ✅ Icon size fixed (standard icon)
3. ✅ MediaKit initialization safe
4. ✅ Supabase errors won't crash app

========================================
📋 REBUILD COMMANDS (COPY & PASTE):
========================================

cd android
.\gradlew --stop
cd ..
flutter clean
flutter pub get
flutter pub run flutter_launcher_icons
flutter build apk --release --dart-define-from-file=supabase-keys.json

========================================

⏱️ EXPECTED TIME: 5-10 minutes

📂 OUTPUT: build\app\outputs\flutter-apk\app-release.apk

========================================
📱 INSTALL ON ANDROID:
========================================

METHOD 1 - ADB:
adb uninstall com.example.flixygo
adb install build\app\outputs\flutter-apk\app-release.apk

METHOD 2 - Manual:
1. Uninstall old FlixyGo from phone
2. Copy app-release.apk to phone
3. Open APK file on phone
4. Install

========================================
✅ WHAT'S FIXED:
========================================

✓ App won't crash on startup
✓ Icon will look normal (not oversized)
✓ Error screens instead of crashes
✓ Works on Android 14

========================================
🐛 IF STILL CRASHING:
========================================

Get crash logs:
adb logcat -c
(open app on phone, let it crash)
adb logcat -d > crash_log.txt

Then share crash_log.txt

========================================
